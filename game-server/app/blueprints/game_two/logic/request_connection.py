


def request_connection(data):
    '''
        Parameters:

            data (dict): Form:

                {
                    'game_id': game_id,
                    'user_id': user_id
                }
    '''

    # game_id = data.get('game_id')


    if user_id in game_i
